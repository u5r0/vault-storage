{
  "name": "vault-storage",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "docker compose up -d && concurrently -n api,web -c \"#a78bfa,#22d3ee\" \"pnpm dev:api\" \"pnpm dev:web\"",
    "dev:web": "pnpm --filter @vault/web dev",
    "dev:api": "tsx watch apps/server/src/index.ts",
    "seed": "tsx apps/server/scripts/seed.ts",
    "build": "pnpm --filter @vault/web build && pnpm --filter @vault/server build",
    "start": "tsx apps/server/src/index.ts",
    "preview": "pnpm --filter @vault/web preview",
    "test": "pnpm test:unit && pnpm test:integration && pnpm test:web",
    "test:unit": "vitest run --project unit",
    "test:integration": "vitest run --project integration",
    "test:integration:r2": "BLOB_PROVIDER=r2 vitest run --project integration",
    "test:web": "vitest run --project web-browser",
    "test:watch": "vitest",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^22.10.0",
    "concurrently": "^9.0.0",
    "msw": "^2.14.6",
    "tsx": "^4.21.0",
    "typescript": "~5.6.3",
    "vitest": "^4.1.6"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
